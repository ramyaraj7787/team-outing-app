# Copy this file to .env.local for local dev, and set the same variable
# in Vercel/Netlify's project settings for your deployed build.

# Local backend:
VITE_API_BASE=http://localhost:5000/api

# Once your API is deployed (e.g. Render), change to something like:
# VITE_API_BASE=https://team-outing-api.onrender.com/api
